#include <string>

namespace thinice {

    class Player {
        string name; // Player's name
        enum color {
            RED,
            GREEN,
            YELLOW,
            BLUE
        };
    };
}