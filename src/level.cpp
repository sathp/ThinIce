#include "level.hpp"

// Constructors
Level::Level() {}
Level::Level(int input[15][19]) {
    for (int i = 0; i < 15; i++) {
        for (int j = 0; j < 19; j++) {
//            maze[i][j] = Block(State(input[i][j]));
        }
    }
}
